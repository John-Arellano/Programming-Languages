function average(num1, num2, num3) {
  suma = num1 + num2 + num3;
  avg = suma / 3;
  console.log(
    "The average of " + num1 + ", " + num2 + ", and " + num3 + " is: " + avg,
  );
}

average(10, 20, 30);
average(5, 15, 25);
average(100, 200, 300);
